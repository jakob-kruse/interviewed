/*
  This function should return true if none of the characters in the string appear more than once.

  Examples:

  allUnique("abcdef") => true
  allUnique("aabbcde") => false
  allUnique("") => false
*/
export function allUnique(value: string): boolean {
  return false;
}
